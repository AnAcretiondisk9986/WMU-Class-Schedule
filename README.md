# WMU 课表 PDF 解析器

面向温州医科大学教务系统导出的横向课表 PDF。解析器直接使用 PDF 文本层和文字坐标，不依赖 OCR。

## 近期更新

### 更新声明（2026-08-17）

本次更新聚焦课表浏览的稳定性与可读性，不改变本地课表数据结构和 PDF 导入协议：

- 宽屏侧栏固定在视口左侧，滚动课程内容时导航始终可用。
- 当前周的当天列以淡红色标注；正在上课的课程卡片以 `#DEBA85` 高亮。
- 竖屏页面的顶栏固定在顶部，搜索与导入入口随时可用。
- 校区作息改为按节次显式配置，不再用固定 45 分钟公式推算：茶山与学院路第 1 节从 `08:00` 开始，滨海第 1 节从 `08:30` 开始；滨海第 7 节为 `13:05–13:20`，三校区第 8 节统一为 `13:30–14:10`、第 14 节统一为 `18:20–19:00`。网页、小程序和跨校区冲突检测使用同一规则。
- 课程地点文字不再被省略或隐藏；定位图标独占居中行，识别出的 4–6 位字母数字教室号单独居中成行并与后续说明分隔，其他地点在等宽文本区域内自然换行。卡片内部采用正常文档流和独立滚动，禁止文本与相邻课程重叠。
- 茶山、滨海地点仅在原始文本开头为 4–6 位字母数字编码时后处理：移除“智慧教室”“计算机机房”等附注；学院路及未识别格式保留原样。
- 新增三页无冲突高强度合成测试 PDF 与可重复生成脚本，覆盖 28 条事件、六种活动类型、极端文本、地点格式和时间边界，可先用于验证课表能否正常导入显示。
- 同一时间发生多门课程时改为稳定分层显示，优先级为“讲课 > 线上 > 无地点 > 学习通等线上平台”；鼠标悬停或键盘聚焦的卡片会临时置顶，便于查看被遮挡内容。
- “讲课”“实验”等课程类型标签统一固定在卡片底部居中，课程正文使用独立滚动区；紧凑与极小卡片也不会隐藏标签或让标签覆盖正文。

### 网页端移动体验

- 移动端锁定页面缩放，避免双指缩放破坏七列课表布局。
- 在周视图中左右拖动可切换上一周或下一周；拖动会跟随手指，快速轻扫可按方向切换，首末可用周次会回弹。
- 左上角校徽现在是导航入口：点击后呼出完整侧栏，底部导航同时向下退出视口；可点击遮罩、关闭按钮或按 `Esc` 返回。
- 周视图表头同时显示「周一至周日」和对应的 `MM/DD` 日期。
- 窄屏课程卡片会按实际高度自动收紧：优先保留课程名与上课时间，逐级隐藏次要信息并限制文本行数，避免文字重叠。
- 为 `prefers-reduced-motion` 提供更轻量的过渡效果。

### 教务系统导入说明

网页端可从应用打开教务系统；首次使用时将弹窗中的「发送课表」链接拖到浏览器书签栏，再在教务系统的「个人课表」页点击该书签。书签脚本会在当前页面及其内嵌框架中查找可信的导出表单，读取 PDF 后安全回传到本页面解析。PDF 不会上传到服务器。

## 微信小程序版

仓库同时提供了微信小程序原型，入口与运行说明见 [MINIPROGRAM.md](MINIPROGRAM.md)。它使用同一套课表解析和校区时间规则，在手机本地解析从微信聊天文件中选择的教务系统 PDF，不依赖后端。

在微信开发者工具中导入仓库根目录即可，`project.config.json` 已将 `miniprogramRoot` 指向 `miniprogram/`。小程序的 PDF.js 与中文 CMap 资源已随代码提供，无需构建 npm；真机兼容性仍需要在实际设备上验证。

## 为什么必须读取坐标

课表 PDF 的文本内容按页面绘制顺序保存，直接拼接文本会丢失“星期几”信息，也会在分页处把不同课程混在一起。解析器先按横坐标恢复星期一至星期日七列，再在每列内解析课程。

该格式中：

- `(2-5节)` 是上课节次。
- `1-2周,4-12周(双),13-17周(单)` 是周次和单双周。
- `◆`、`◇`、`▲`、`●`、`#`、`□` 分别表示讲课、实验、在线、讨论、实践和自主学习。

## 使用

```js
import * as pdfjsLib from "pdfjs-dist/legacy/build/pdf.mjs";
import { parseTimetablePdf } from "./src/timetable.js";

const result = await parseTimetablePdf(file, pdfjsLib);
console.log(result.courses);
console.log(result.events);
```

`file` 可以是浏览器 `File`、`ArrayBuffer` 或 `Uint8Array`。结果包含学期、学生信息、合并后的课程列表、逐条上课事件和解析警告。

## 从教务系统获取 PDF

网页的「导入课表」弹窗提供无扩展的教务系统桥接流程：

1. 从应用打开个人课表查询页并完成统一认证。
2. 首次使用时，将弹窗中的「发送课表到本页面」链接拖入浏览器书签栏。
3. 在教务系统课表页点击该书签。脚本在 `jwxt.wmu.edu.cn` 同源上下文提交页面已有的 PDF 表单，再通过 `postMessage` 把 PDF 返回应用并自动解析。

接收端会校验消息来源、窗口引用、PDF 文件签名和大小。PDF 与课表数据仍只在用户浏览器中处理，不会上传到项目服务器。由于浏览器同源策略限制，纯静态站点无法在用户登录后自动注入教务系统页面，因此无扩展方案仍需要用户点击一次书签；本地 PDF 导入继续作为兼容路径。

## 测试

真实样本默认放在 `test/fixtures/黄映焜(2026-2027-1)课表.pdf`（滨海校区）和 `test/fixtures/崔艺鑫(2026-2027-1)课表.pdf`（茶山校区）。由于含有个人信息，PDF 已被 `.gitignore` 排除。

仓库还提供不含真实排课关系的 `output/pdf/WMU课表-高强度导入测试.pdf`。它覆盖三校区、六种活动类型、单双周与多区间周次、第 7/8 节边界、长课程/教师文本，以及短教室号、带说明教室号和自由换行地点；所有课程时间均已安排为零跨校区冲突，可先用于验证导入与显示。可运行以下命令重新生成：

```powershell
python scripts/generate_stress_test_pdf.py
```

```powershell
pnpm install
pnpm test
```

当前测试覆盖 PDF 解析、单双周与跨校区冲突、网页渲染/持久化/多课表切换、书签脚本来源校验，以及移动端周表头、侧栏状态和横向切换周次手势。
